# 代码检视报告

## 概述
- **检视范围**: `64655be..a75ff28`（Phase 1-3，从初始提交到 HEAD）
- **文件变更**: 6 个文件变更（Phase 3 增量）+ 约 30 个文件（整个项目）
- **整体评价**: **建议修改** — 核心架构良好，测试覆盖率高，但存在一个关键问题（start_member 工具未实际创建 Member 进程）和若干架构偏差。

---

## 功能分析

### Phase 1-2（初始提交 `64655be`）：项目骨架，团队定义，命令，进程管理

**已完成的功能：**
- 团队定义 YAML 类型定义与校验（`src/team/definition.ts`, `schema.ts`）
- 团队定义文件的读写删（`src/team/store.ts`）
- 4 个基本命令（`team-list`, `team-show`, `team-delete`, `team-status`）
- 4 个 TL 进程管理工具的注册框架（`tl-tools.ts`）
- Member 进程包装器（`member-process.ts`）
- 进程管理器（`process/manager.ts`）
- 会话状态管理（`session/state.ts`）
- Team Lead 侧扩展入口（`index.ts`）含 `/team start` `/team stop` 命令
- Member 侧扩展入口（`member.ts`）含 `team_send_message` 工具和角色提示注入

**设计中提及但尚未实现的功能：**
- `src/prompt/tl-system-prompt.ts` / `member-env.ts` — 提示词内联在 `index.ts` 和 `member.ts` 中
- `src/tools/member-tool.ts` — `team_send_message` 实现在 `member.ts` 中
- `/team create`/`/team start` 命令对应 create/start 命令（已实现）
- `get_member_log` 工具尚未真正通过 RPC `get_messages` 读取日志

### Phase 3（`683af08` + `a75ff28`）：消息通道基础设施

**已完成的功能：**
- `src/channel/types.ts` — `TeamMessage` 接口和 `TeamMessageHandler` 类型
- `src/channel/message-queue.ts` — 串行 FIFO 消息队列
- `src/channel/router.ts` — 消息路由（to member / tl / all）
- `index.ts` — 消息通道集成：创建 queue / router，在 `tool_execution_end` 中捕获 `team_send_message` 事件，enqueue 消息

### 功能差距
| 设计需求 | 实现状态 | 差距 |
|----------|----------|------|
| start_member 实际创建并启动 Member 进程 | ❌ 未实现 | execute 只返回文本确认，不调用 createMember |
| get_member_log 通过 RPC get_messages 读取日志 | ❌ 待实现 | 返回占位文本"需在 Phase 4 中实现" |
| auto-restart 被正确触发 | ⚠️ 部分实现 | manager.handleExit 逻辑可能因 handles 未同步而失效 |
| 独立 member-tool.ts 文件 | ❌ 内联实现 | 实现在 member.ts 中，非独立模块 |
| prompt/ 目录 | ❌ 缺失 | 提示词逻辑内联在 index.ts / member.ts |
| E2E 测试 | ❌ 缺失 | 设计中 Phase 3 step 22 要求 E2E 测试 |

---

## 实现方案评估

### 架构一致性

整体架构与 DESIGN.md 和 CONTEXT.md 高度一致：
- **TL-Member 通信**：通过 `pi --mode rpc` 的 stdin/stdout JSONL 协议 ✓
- **消息通道**：全局 FIFO 队列 + 路由器，无外部基础设施 ✓
- **成员进程生命周期**：通过 `member-process.ts` + `manager.ts` 管理 ✓
- **环境变量传递角色信息**：通过 `process.env.TEAM_*` ✓
- **扩展模式检测**：TL 侧通过 `ctx.mode !== "rpc"`，Member 侧通过 `process.env.TEAM_ROLE` ✓

### 关键架构偏差

1. **`start_member` 工具是空操作**（CRITICAL）
   - 在 `index.ts` 的 `setupTlTools()` 中，`createMember` 回调注册了 handle 创建 + 事件绑定逻辑，但该回调从未被调用
   - `registerTlTools` 内部不调用 `createMember`，`start_member` 的 `execute` 只返回文本
   - 结果：TL 调用 `start_member("analyzer")` 后无实际进程创建，消息通道无法工作

2. **`processManager` 与 `memberHandles` 未同步**（CRITICAL）
   - `createProcessManager([])` 以空数组初始化，成员 handle 被放入 `memberHandles` Map
   - `manager.stop(name)`、`manager.listStatus()`、`manager.getStatus()` 均依赖内部 `memberMap`
   - 所有 tools（`stop_member`, `list_members`, `get_member_log`）调用 `manager` 方法时将找不到 handle
   - `manager.handleExit()` 也在空 map 中查找，自动重启不会触发

3. **`src/prompt/` 和 `src/tools/member-tool.ts` 缺失**
   - 提示词逻辑内联，不影响功能但降低了可测试性和模块化
   - 这是设计中明确的结构，偏差应记录

### 方案评估

消息通道实现方案轻量且正确，选择纯内存 FIFO 队列 + 路由器是恰当的，避免了外部依赖：

| 选型 | 评估 |
|------|------|
| 内存队列 vs 外部消息代理 | ✅ 正确选择，减少部署复杂度和延迟 |
| 串行处理 vs 并发 | ✅ 串行避免了消息乱序，符合设计 |
| `queueMicrotask` 延迟处理 | ✅ 让 enqueue 先返回再处理 |
| 通过 `tool_execution_end` 拦截消息 | ✅ 标准 RPC 事件模式，无需额外协议 |

---

## 代码流程梳理

### 消息通道流程（设计预期）

```
Member A 调用 team_send_message()
  → RPC stdout 输出 tool_execution_end 事件
  → TL 侧 member-process stdout 监听器收到事件
  → index.ts 中检查 toolName === "team_send_message"
  → 从 event.result.details.teamMessage 提取消息
  → messageQueue.enqueue(msg)
  → 队列串行处理：router.route(msg)
  → router 根据 msg.to 分发：
      "analyzer" → sendToMember("analyzer", msg)
                   → memberHandles.get("analyzer")?.sendCommand(...)
                   → 写入 Member B 的 RPC stdin
      "tl"       → sendToTl(msg)
                   → pi.sendMessage(...) 注入 TL 对话
      "all"      → 遍历所有 member，跳过发送者
```

### 实际流程中断点

```
TL 调用 start_member("analyzer")
  → start_member.execute() 返回文本 "启动请求已提交"
  → ❌ 未调用 createMember(config)
  → memberHandles 中无 "analyzer" 条目
  → 后续消息通道无法发送给该成员
  → member-process stdout 监听器未注册
  → 无法拦截 tool_execution_end 事件
```

---

## 多维度质量检查

### 严重问题（必须修复）

| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 1 | `index.ts` | 151-162 | **功能缺陷** | `setupTlTools()` 中的 `createMember` 回调注册了 handle 创建和事件绑定，但 `registerTlTools` 内部不调用该回调；`start_member` 工具的 `execute` 仅返回文本而不实际创建进程 | 在 `start_member.execute` 中调用 `createMember(config)`；或在 `index.ts` 的 `/team start` handler 中预创建所有成员，而非依赖工具调用 |
| 2 | `index.ts` | 141, 151-169 | **功能缺陷** | `createProcessManager([])` 以空数组初始化，成员 handles 存放在 `memberHandles` Map 而非 `processManager` 内部。`manager.stop()`、`listStatus()`、`getStatus()` 均无法找到成员 | 方案 A：将 `memberHandles` 中的 handle 也添加到 `processManager`（需扩展 `createProcessManager` 支持动态添加 handle）；方案 B：在 `start_member` 中将 handle 同时加入 `memberHandles` 和 `processManager` |

### 警告（建议修复）

| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 3 | `src/channel/router.ts` | 27-28 | **代码坏味** | `updateMembers` 直接修改外部传入数组（`memberNames.length = 0; memberNames.push(...names)`），产生副效应 | 改用内部克隆或重新赋值，避免修改调用方引用 |
| 4 | `src/channel/message-queue.ts` | 27-29 | **健壮性** | handler 错误被静默吞噬（`catch {}`），调试困难 | 至少输出 `console.warn("[team-queue] handler error:", err)` |
| 5 | `src/tools/tl-tools.ts` | 94 | **日志争议** | `list_members` 返回 "没有活跃的团队成员" 但可能有已注册但未启动的成员 | 应区分 "无成员" vs "成员已注册但未启动" |
| 6 | `src/process/manager.ts` | 10-11 | **接口设计** | `handles` 数组在构造后无法添加新 handle，导致 `start_member` 动态创建的 handle 无法被 manager 管理 | 添加 `addHandle(handle)` 方法或重构为动态管理 |
| 7 | `member.ts` | 64 | **健壮性** | `content.slice(0, 200)` 没有说明截断，内容超长时 Member 可能未意识到消息被截断 | 添加截断提示 "..." 已有，但应明确告知消息长度限制 |
| 8 | `src/process/member-process.ts` | 119-124 | **健壮性** | `spawn` 可能抛出同步异常（如命令不存在），异步 `start()` 会 reject 但 `memberHandles` 中没有清理 | 在启动失败时重置 `status` 为 "stopped" 并确保资源清理 |

### 建议（可考虑）

| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 9 | `src/channel/message-queue.ts` | 56-65 | **性能** | `drain()` 使用 `setTimeout(r, 5)` 轮询，测试中可接受，生产环境下可能有微秒级延迟 | 改用 Promise 链或事件驱动模式，避免忙等 |
| 10 | `index.ts` | 190 | **健壮性** | `event.result?.details?.teamMessage` 多层可选链，但 `teamMessage` 结构未做校验 | 添加类型守卫或 Zod 校验，防止畸形数据 |
| 11 | `src/channel/types.ts` | 16 | **类型** | `TeamMessageHandler` 类型为 `void`，但 `message-queue.ts` 中 `await handler(msg)` 暗示其为 `Promise<void>` | 改为 `(msg: TeamMessage) => void | Promise<void>` |
| 12 | 全局 | — | **架构** | `src/prompt/` 目录和 `src/tools/member-tool.ts` 缺失 | 考虑按设计重构为独立模块，提高可测试性和可维护性 |

---

## 测试覆盖分析

### TDD 合规性

| 模块 | 测试文件 | 测试数 | 覆盖率符合度 |
|------|----------|--------|-------------|
| `team/definition.ts` | `definition.test.ts` | 2 | ✅ 类型解析 |
| `team/schema.ts` | `schema.test.ts` | 8 | ✅ 边界覆盖完整 |
| `team/store.ts` | `store.test.ts` | 9 | ✅ 读写删列全覆盖 |
| `commands/list|show|delete` | `basic-commands.test.ts` | 7 | ✅ 注册+边界 |
| `commands/status` | `status.test.ts` | 3 | ✅ 活跃/非活跃 |
| `process/manager.ts` | `manager.test.ts` | 7 | ✅ 各方法+auto-restart |
| `process/member-process.ts` | `member-process.test.ts` | 6 | ✅ spawn参数/exit/stop/event |
| `tools/tl-tools.ts` | `tl-tools.test.ts` | 5 | ✅ 4工具注册 |
| `channel/message-queue.ts` | `message-queue.test.ts` | 5 | ✅ FIFO/串行/错误容忍/stop |
| `channel/router.ts` | `router.test.ts` | 7 | ✅ 路由逻辑全覆盖 |
| `session/state.ts` | 无专用测试 | 0 | ⚠️ 通过 `status.test.ts` 间接测试 |

**总体：11 个测试文件，60 个测试，全部通过 ✅**

**缺失测试：**
- `member.ts` 的 `team_send_message` 工具无专用测试（虽然有 `member-process.test.ts` 间接测试事件拦截）
- `index.ts` 的集成逻辑无测试（commands/team-start/stop、message channel wiring）
- `session/state.ts` 无专用单元测试
- 无 E2E 测试

### 测试质量评价

测试总体质量高：
- 使用临时目录（`mkdtempSync`）避免测试间污染
- Mock 策略合理（mock `spawn`, mock `ExtensionAPI`）
- 边界覆盖好（空 teams 目录、非 YAML 文件、重复名称、错误字符、无效输入）
- `message-queue.test.ts` 的串行执行测试（验证 `inFlight.length === 1`）设计精巧

---

## 代码优化建议

### 优先级最高的修复：启动流程重连

核心问题在于 `start_member` 工具、`memberHandles` 和 `processManager` 三者未正确联动。建议的最小修复方案：

```typescript
// 在 index.ts 中：
// 1. 让 processManager 和 memberHandles 共享同一份 handle 数据
// 2. 重写 start_member 工具的实际执行逻辑

// tl-tools.ts 中 start_member 的 execute 应实际调用 createMember:
async execute(_toolCallId, params) {
  const config = buildMemberConfig(params.name, teamDefinition);
  const handle = createMember(config);  // 实际创建进程
  manager.addHandle(handle);            // 加入管理器
  await handle.start();                 // 启动进程
  return { content: [...] };
}
```

### 架构改进建议

1. **动态添加 handle 到 ProcessManager**：添加 `addHandle(handle)` 方法
2. **统一 handle 存储**：将 `memberHandles` Map 合并到 `processManager`，或反之
3. **消息处理失败可视化**：添加简要日志，帮助调试

---

## 总结

### 正面评价
- ✅ **架构设计清晰**：严格遵循 DESIGN.md 的架构，TL-Member 分离、消息通道、env 传递角色信息等设计决策一致
- ✅ **测试覆盖率高**：60 个测试全部通过，单元测试覆盖核心逻辑模块的边界情况
- ✅ **代码质量好**：TypeScript 严格模式、ES2022 目标、模块化组织、合理使用 async/await
- ✅ **消息通道实现健壮**：串行队列、错误容忍、正确支持 FIFO、路由逻辑覆盖到/member/all/tl/self
- ✅ **扩展点预留良好**：`spawnFn` 可注入、`piCommand` 可配置、`TOP_NOTCH_TEAM_ROOT` 可覆盖
- ✅ **类型安全**：接口定义清晰（`TeamMessage`, `TeamDefinition`, `MemberProcessConfig`）

### 需要修复的问题
| 严重级别 | 数量 | 核心问题 |
|----------|------|----------|
| 🔴 CRITICAL | 2 | `start_member` 不创建进程；`processManager` 与 `memberHandles` 未同步 |
| 🟡 WARNING | 6 | 副效应修改外部数组、错误静默、架构偏差等 |
| 🔵 INFO | 4 | 类型精化、模块化重构等 |

### 整体判断
项目处于 Phase 3 早期，核心基础设施（消息队列、路由、事件拦截）已就绪并正确连接到扩展入口。但 **Phase 2 的进程创建链路存在断点**，导致 Phase 3 的消息通道在运行态无法实际工作。建议在进入 Phase 4 前修复 CRITICAL 问题，否则端到端测试将失败。
