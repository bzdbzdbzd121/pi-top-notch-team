# 代码检视报告

## 概述
- **检视范围**: `e90060a` feat: add team_send_and_wait tool with ResponseWaiter
- **文件变更**: 7 个文件变更, 479 行新增(+), 7 行删除(-)
- **整体评价**: 建议修改（存在 2 个严重问题）

## 功能分析
新增 `team_send_and_wait` 工具，支持 TL 向 Member 发送消息后阻塞等待回复，通过 correlation ID 匹配响应消息。
同时新增 `ResponseWaiter` 模块管理等待器的注册、匹配、超时和取消。

## 实现方案评估
架构设计合理：利用工具 `execute()` 返回 Promise 的自然阻塞特性，配合消息通道的 `sendToTl` 回调拦截，实现不轮询的等待机制。correlation ID 嵌入消息内容的设计支持链式交接场景。 

## 代码流程梳理

```
TL team_send_and_wait → enqueue(含<corr:xxx>) → register waiter → Promise pending
Member 回复 → tool_execution_end → enqueue → router.route() → sendToTl
  └─ extractCorrelationId → resolveIfWaiting → resolve Promise → 工具返回结果
```

## 多维度质量检查

### 严重问题（必须修复）
| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 1 | `index.ts` | (约 225 行附近) | **功能缺失** | `pi.registerTool({ name: "team_send_and_wait", ... })` 没有被执行。`tlToolNames` 列表中已有此名称、TL 系统提示中也有描述，但实际**工具未注册**——Python 脚本的替换模式因注释格式不一致而未能匹配 | 在 `registerTlTools` 调用之后添加 `pi.registerTool({ name: "team_send_and_wait", ... })` 调用 |
| 2 | `src/commands/team.ts` | 339-363 (`/team stop`) | **功能缺失** | `/team stop` 终止团队会话时没有调用 `responseWaiter.cancelAll()`，导致如果有 pending 的等待器，它们会等到超时后才 resolve，浪费 Promise 资源 | 在 stop 处理器中调用 `teamCtx.responseWaiter?.cancelAll()`。注意 `teamCtx.responseWaiter` 在 `session/context.ts` 中已是必选字段，确保定义即可 |

### 警告（建议修复）
| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 3 | `src/channel/response-waiter.ts` | 107-110 | **测试缺口** | `extractCorrelationId()` 函数已导出并用于 `sendToTl` 的关键路径，但没有专属单元测试 | 添加测试覆盖：正常匹配、不匹配、空字符串、边界字符（下划线、连字符） |

### 建议（可考虑）
| # | 文件 | 行号 | 类型 | 问题描述 | 修改建议 |
|---|------|------|------|----------|----------|
| 4 | `index.ts` | (sendToTl) | **行为考量** | 当 `resolveIfWaiting` 为 true 时，回复消息直接作为工具结果返回，不会通过 `pi.sendMessage()` 进入 TL 的对话上下文。如果 TL 想看到原始回复内容，只有工具返回的截断文本 | 考虑在工具返回完整 content 的同时，添加 `details.rawContent` 字段。不过当前设计（"回复不进入上下文"）是用户明确要求的，当前行为正确 |
| 5 | `src/channel/response-waiter.ts` | 21 | **命名** | `PendingEntry.correlationId` 字段存储了冗余数据（key 本身就是 corrId），从未被读取 | 移除 `PendingEntry.correlationId` 字段 |

## 代码优化建议
- `response-waiter.ts` 整体实现清晰、边界处理完整（超时、取消、idempotent）
- `sendToTl` 中的 waiter 检查路径正确：先检查 `msg.correlationId` 字段，再 fallback 到 `extractCorrelationId` 内容扫描

## 总结
**两个必须修复的问题**：`team_send_and_wait` 工具未注册（功能完全不可用）、`cancelAll` 未在 stop 时调用。修复后功能完整，架构设计合理，ResponseWaiter 模块测试覆盖率良好。
